---
phase: 02-drafts-and-tags
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - blogmaker.py
  - tests/test_build.py
autonomous: true
requirements:
  - DRFT-01
  - DRFT-02

must_haves:
  truths:
    - "A post with `Draft: true` header is parsed with draft=True in the post dict"
    - "Draft posts produce no HTML output file in docs/"
    - "Draft posts do not appear in the index page post list"
    - "A previously published post that becomes a draft has its stale HTML deleted from docs/"
    - "Existing posts without a Draft header build identically (draft defaults to False)"
  artifacts:
    - path: "blogmaker.py"
      provides: "Draft parsing in _parse_post() and draft filtering in build()"
      contains: "draft"
    - path: "tests/test_build.py"
      provides: "Draft support tests"
      contains: "draft"
  key_links:
    - from: "blogmaker.py _parse_post()"
      to: "headers dict"
      via: "headers.get('draft', '').lower() == 'true'"
      pattern: "headers\\.get\\('draft'"
    - from: "blogmaker.py build()"
      to: "draft filtering"
      via: "list comprehension filtering draft posts"
      pattern: "not.*\\['draft'\\]"
    - from: "blogmaker.py build()"
      to: "stale file cleanup"
      via: "unlink for draft post HTML files"
      pattern: "unlink"
---

<objective>
Add draft post support to the blog engine. Posts with a `Draft: true` header are excluded from HTML generation, excluded from the index page, and have any stale output files cleaned up.

Purpose: Authors can work on posts without publishing them to the live site. This is a prerequisite for the tag system (Plan 02) since tag pages must also exclude drafts.
Output: Modified blogmaker.py with draft parsing and filtering, plus comprehensive tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-drafts-and-tags/02-CONTEXT.md
@.planning/phases/02-drafts-and-tags/02-RESEARCH.md
@blogmaker.py
@tests/test_build.py

<interfaces>
<!-- Key code the executor needs to modify -->

From blogmaker.py _parse_post() (lines 96-177):
- Header parser (lines 102-118) already recognizes 'draft' in known_headers
- Post dict (lines 163-177) already has 'draft': False placeholder at line 175

From blogmaker.py build() (lines 241-331):
- Post collection loop: lines 263-288
- Index rendering: lines 312-317 (uses full `posts` list)
- Cache save: line 328

From tests/test_build.py:
- _make_config(tmpdir) helper creates isolated test environment
- Tests use tempfile.TemporaryDirectory() pattern
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Draft parsing and filtering with tests</name>
  <files>blogmaker.py, tests/test_build.py</files>
  <read_first>blogmaker.py, tests/test_build.py</read_first>
  <behavior>
    - Test: Post with "Draft: true" header has draft=True in parsed dict
    - Test: Post with "Draft: True" (capitalized) has draft=True (case-insensitive per D-01)
    - Test: Post with "Draft: TRUE" has draft=True (case-insensitive per D-01)
    - Test: Post without Draft header has draft=False (backward compatibility)
    - Test: Draft post produces no HTML output file in docs/ after build (per D-02)
    - Test: Draft post does not appear in index.html post listing (per D-02)
    - Test: Non-draft post still builds normally alongside a draft post
  </behavior>
  <action>
    **In blogmaker.py _parse_post() (around line 175):**
    Replace `'draft': False,` with actual parsing from headers dict per D-01:
    ```python
    'draft': headers.get('draft', '').lower() == 'true',
    ```

    **In blogmaker.py build() (after post collection loop, before rendering):**
    Per D-02, separate published from draft posts:
    ```python
    published = [p for p in posts if not p['draft']]
    ```

    **In the post rendering loop (lines 263-288):**
    Skip HTML generation for draft posts. Add a condition: if the post is a draft, skip rendering and print a draft notice instead. Do NOT add draft posts to the cache.

    **In index rendering (lines 312-317):**
    Use `published` instead of `posts` for the index template render call.

    **Stale file cleanup per D-03:**
    After the post collection loop, iterate over draft posts and delete any existing HTML output file:
    ```python
    for post in posts:
        if post['draft']:
            stale = output_path / f"{post['slug']}.html"
            if stale.exists():
                stale.unlink()
                print(f"  x {post['title']} (draft, removed)")
    ```

    **In tests/test_build.py:**
    Write RED tests first, then implement. Group draft tests together. Use the existing _make_config pattern. Tests to add:
    1. test_draft_post_excluded_from_output - create a draft post, build, assert no HTML file
    2. test_draft_case_insensitive - test "True", "TRUE", "true" all work
    3. test_draft_excluded_from_index - build with draft + non-draft, check index HTML does not contain draft title
    4. test_non_draft_builds_normally - a post without Draft header still produces HTML
    5. test_stale_draft_cleanup - build a post (creates HTML), then make it a draft and rebuild, assert HTML is gone
    6. test_draft_post_not_in_posts_list - verify the published list filtering

    IMPORTANT: Run tests after writing them to confirm RED, then implement, then confirm GREEN.
  </action>
  <verify>
    <automated>cd /Users/pili/blog_project && python3 -m py_compile blogmaker.py && python3 tests/test_build.py && python3 tests/test_quality.py</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "headers.get('draft'" blogmaker.py
    - grep -q "not.*draft" blogmaker.py
    - grep -q "unlink" blogmaker.py
    - grep -q "def test_draft" tests/test_build.py
    - python3 tests/test_build.py 2>&1 | grep -q "OK"
    - python3 tests/test_quality.py 2>&1 | grep -q "OK"
  </acceptance_criteria>
  <done>
    Draft posts with `Draft: true` (case-insensitive) are excluded from HTML output and index page.
    Stale HTML files are deleted when a post becomes a draft.
    Existing posts without Draft header build identically.
    All new tests pass, all existing tests pass, py_compile succeeds.
  </done>
</task>

</tasks>

<verification>
1. `python3 -m py_compile blogmaker.py` -- no syntax errors
2. `python3 tests/test_build.py` -- all tests pass including new draft tests
3. `python3 tests/test_quality.py` -- quality checks still pass
4. `python3 blogmaker.py` -- full build succeeds with existing posts (none have Draft header, so no behavior change)
</verification>

<success_criteria>
- Draft header parsing works case-insensitively (true/True/TRUE)
- Draft posts produce no HTML and do not appear in index
- Stale HTML cleanup works when a published post becomes a draft
- All existing tests pass (no regressions)
- All new draft tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-drafts-and-tags/02-01-SUMMARY.md`
</output>
