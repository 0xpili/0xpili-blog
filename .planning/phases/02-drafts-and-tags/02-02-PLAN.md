---
phase: 02-drafts-and-tags
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - blogmaker.py
  - templates/base.html
  - templates/index.html
  - tests/test_build.py
autonomous: false
requirements:
  - TAGS-01
  - TAGS-02
  - TAGS-03

must_haves:
  truths:
    - "A post with `Tags: crypto, ai` header is parsed with tags=['crypto', 'ai'] in the post dict"
    - "Tags appear as comma-separated links on individual post pages"
    - "Tag links point to the correct tag index page URL (tag-{name}.html)"
    - "Per-tag index pages are generated listing only non-draft posts with that tag"
    - "Tag index pages show posts sorted newest-first"
    - "Stale tag pages from previous builds are cleaned up"
    - "Existing posts without Tags header build with empty tags list"
  artifacts:
    - path: "blogmaker.py"
      provides: "Tag parsing in _parse_post() and tag page generation in build()"
      contains: "tag"
    - path: "templates/base.html"
      provides: "Tag display on post pages"
      contains: "tags"
    - path: "templates/index.html"
      provides: "Conditional heading for tag pages vs homepage"
      contains: "tag"
    - path: "tests/test_build.py"
      provides: "Tag system tests"
      contains: "tag"
  key_links:
    - from: "blogmaker.py _parse_post()"
      to: "headers dict"
      via: "comma-split, strip, lowercase tag parsing"
      pattern: "split.*,.*strip.*lower"
    - from: "blogmaker.py build()"
      to: "templates/index.html"
      via: "render call with tag parameter for tag pages"
      pattern: "tag="
    - from: "blogmaker.py build()"
      to: "post_template.render()"
      via: "tags=post['tags'] passed to template"
      pattern: "tags=post"
    - from: "templates/base.html"
      to: "tag-{name}.html"
      via: "tag link hrefs in .meta div"
      pattern: "tag-.*\\.html"
    - from: "templates/index.html"
      to: "conditional heading"
      via: "{% if tag %} block"
      pattern: "if tag"
---

<objective>
Add tag system to the blog engine. Posts can specify tags via a `Tags:` header. Tags are displayed as links on post pages and generate per-tag index pages listing all non-draft posts with that tag.

Purpose: Readers can discover related content by topic. Tag pages provide browsable topic groupings.
Output: Modified blogmaker.py with tag parsing and generation, updated templates, and comprehensive tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-drafts-and-tags/02-CONTEXT.md
@.planning/phases/02-drafts-and-tags/02-RESEARCH.md
@.planning/phases/02-drafts-and-tags/02-01-SUMMARY.md
@blogmaker.py
@templates/base.html
@templates/index.html
@tests/test_build.py

<interfaces>
<!-- Key code the executor needs. Plan 01 will have already added draft support. -->

From blogmaker.py _parse_post() post dict:
- 'tags': [] placeholder at line 176 -- replace with parsed value
- Header parser already recognizes 'tags' in known_headers

From blogmaker.py build():
- `published` list (added by Plan 01) -- use for tag page generation
- Post rendering loop passes kwargs to post_template.render() -- add tags=

From templates/base.html line 255:
- `.meta` div: `<div class="meta">{{ date }}</div>` -- add tag links here

From templates/index.html lines 135-136:
- `<h1>0xpili</h1>` and `<p class="tagline">` -- wrap in conditional for tag pages
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tag parsing, display, and index page generation</name>
  <files>blogmaker.py, templates/base.html, templates/index.html, tests/test_build.py</files>
  <read_first>blogmaker.py, templates/base.html, templates/index.html, tests/test_build.py</read_first>
  <behavior>
    - Test: Post with "Tags: crypto, ai" has tags=['crypto', 'ai'] in parsed dict (TAGS-01)
    - Test: Tags are whitespace-trimmed and lowercased ("  Crypto , AI " -> ['crypto', 'ai']) (per D-04)
    - Test: Post without Tags header has tags=[] (backward compatibility)
    - Test: Tag links appear in post HTML output with correct href (TAGS-02)
    - Test: Per-tag index pages are generated at docs/tag-{name}.html (TAGS-03, D-06)
    - Test: Tag index page contains only posts with that tag, not others
    - Test: Tag index page heading shows "Posts tagged: {tag}" (per D-07)
    - Test: Draft posts do not appear in tag index pages (Pitfall 1)
    - Test: Stale tag pages are cleaned up when tags are removed (Pitfall 2)
    - Test: Tag pages list posts sorted newest-first (per D-07)
  </behavior>
  <action>
    **Step 1: Write RED tests in tests/test_build.py** for all behaviors above. Run to confirm failure.

    **Step 2: Implement tag parsing in blogmaker.py _parse_post():**
    Replace `'tags': [],` (line 176) with parsed value per D-04:
    ```python
    tags_raw = headers.get('tags', '')
    tags = [t.strip().lower() for t in tags_raw.split(',') if t.strip()] if tags_raw else []
    # In the return dict:
    'tags': tags,
    ```

    **Step 3: Pass tags to post template render in build():**
    In the post_template.render() call (around line 270-277 after Plan 01 changes), add `tags=post['tags']` to the keyword arguments. Per Pitfall 3, this is required for tags to display.

    **Step 4: Update templates/base.html for tag display (per D-05):**
    Replace the `.meta` div (line 255) with:
    ```html
    <div class="meta">
      {{ date }}
      {% if tags %}
      <span class="tags"> · {% for tag in tags %}<a href="/tag-{{ tag }}.html">{{ tag }}</a>{% if not loop.last %}, {% endif %}{% endfor %}</span>
      {% endif %}
    </div>
    ```
    Add minimal CSS for `.tags` in the `<style>` block -- no new selectors needed since tags use existing `a` styles. The `.tags` span just provides semantic grouping.

    **Step 5: Update templates/index.html for tag page conditional (per D-07):**
    Wrap the h1 and tagline in a conditional per Pattern 6 from research:
    ```html
    {% if tag %}
    <h1>Posts tagged: {{ tag }}</h1>
    {% else %}
    <h1>0xpili</h1>
    <p class="tagline">On crypto, AI, and being human</p>
    {% endif %}
    ```

    **Step 6: Generate tag index pages in build():**
    After index rendering and using the `published` list (from Plan 01):

    1. Clean stale tag pages first (Pitfall 2):
       ```python
       for old_tag_page in output_path.glob("tag-*.html"):
           old_tag_page.unlink()
       ```

    2. Collect tags from published posts:
       ```python
       tag_posts = {}
       for post in published:
           for tag in post['tags']:
               tag_posts.setdefault(tag, []).append(post)
       ```

    3. Generate one page per tag per D-06:
       ```python
       for tag, tagged in tag_posts.items():
           tagged.sort(key=lambda x: x['date_obj'], reverse=True)
           tag_html = index_template.render(posts=tagged, tag=tag)
           (output_path / f"tag-{tag}.html").write_text(tag_html, encoding='utf-8')
           print(f"  + Tag: {tag} ({len(tagged)} posts)")
       ```

    Per Pitfall 5, always regenerate all tag pages on every build (no caching).

    **Step 7: Run tests to confirm GREEN.** Then run full suite and py_compile.
  </action>
  <verify>
    <automated>cd /Users/pili/blog_project && python3 -m py_compile blogmaker.py && python3 tests/test_build.py && python3 tests/test_quality.py</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "split.*," blogmaker.py (tag parsing)
    - grep -q "tags=post" blogmaker.py (tags passed to template)
    - grep -q "tag-.*\.html" blogmaker.py (tag page generation)
    - grep -q "tag_posts" blogmaker.py (tag collection)
    - grep -q "tag-.*\.html" templates/base.html (tag links)
    - grep -q "if tag" templates/index.html (conditional heading)
    - grep -q "Posts tagged" templates/index.html (tag page heading)
    - grep -q "def test_tag" tests/test_build.py (tag tests exist)
    - python3 tests/test_build.py 2>&1 | grep -q "OK"
    - python3 tests/test_quality.py 2>&1 | grep -q "OK"
  </acceptance_criteria>
  <done>
    Tags parsed from header as comma-separated, trimmed, lowercased list.
    Tags display as links on post pages pointing to tag-{name}.html.
    Per-tag index pages generated with correct heading and filtered post list.
    Draft posts excluded from tag pages.
    Stale tag pages cleaned up on rebuild.
    All tests pass, quality checks pass, py_compile succeeds.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of tags</name>
  <files>none</files>
  <read_first>none</read_first>
  <action>
    Present the tag system for user visual verification. Add `Tags: crypto, ai` to a test post header, rebuild with `python3 blogmaker.py`, and start dev server with `python3 serve.py`.
  </action>
  <what-built>Tag display on post pages and tag index pages. Tags appear as pink accent links in the .meta section of each post, and each tag links to a tag-{name}.html page showing filtered posts.</what-built>
  <how-to-verify>
    1. Add `Tags: crypto, ai` to a test post header and rebuild: `python3 blogmaker.py`
    2. Run `python3 serve.py` and open http://localhost:8000
    3. Click on a post with tags -- verify tags appear below the date as linked text (e.g., "crypto, ai")
    4. Click a tag link -- verify it goes to a tag index page with heading "Posts tagged: crypto"
    5. Verify the tag page lists only posts with that tag, sorted newest-first
    6. Verify dark mode toggle still works on tag pages
    7. Verify index page still shows normal heading (not tag heading)
  </how-to-verify>
  <verify>User approves visual appearance of tag links and tag index pages</verify>
  <acceptance_criteria>
    - User types "approved"
  </acceptance_criteria>
  <done>Tag display and tag index pages visually verified by user</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `python3 -m py_compile blogmaker.py` -- no syntax errors
2. `python3 tests/test_build.py` -- all tests pass including tag tests
3. `python3 tests/test_quality.py` -- quality checks still pass (index <10KB, posts <25KB)
4. `python3 blogmaker.py` -- full build succeeds, tag pages generated for any posts with tags
5. Visual: tag links appear on post pages, tag index pages are browsable
</verification>

<success_criteria>
- Tags parsed correctly from header (comma-separated, trimmed, lowercased)
- Tags display as links on post pages
- Per-tag index pages generated at tag-{name}.html
- Tag pages exclude draft posts
- Stale tag pages cleaned up
- All existing and new tests pass
- Quality checks pass (size budgets maintained)
- Visual verification approved by user
</success_criteria>

<output>
After completion, create `.planning/phases/02-drafts-and-tags/02-02-SUMMARY.md`
</output>
