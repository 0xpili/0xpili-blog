Build the blog and run quality checks.

Steps:
1. Run `python3 blogmaker.py` to build the site
2. Run `python3 tests/test_quality.py` to verify quality standards
3. Report the results: any rebuilt posts, page sizes, and test outcomes
4. If any quality tests fail, investigate and suggest fixes